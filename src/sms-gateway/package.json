{
  "name": "@penny/sms-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Penny SMS gateway — explore + execute over text",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "predev": "npm run build -w @penny/core && npm run build -w @penny/api-client && npm run build -w @penny/agent",
    "dev": "tsx --env-file=../.env src/index.ts",
    "pretypecheck": "npm run build -w @penny/core && npm run build -w @penny/api-client && npm run build -w @penny/agent",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.17",
    "@penny/agent": "*",
    "@penny/api-client": "*",
    "@penny/core": "*",
    "hono": "^4.13.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
